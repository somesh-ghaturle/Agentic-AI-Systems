# Pinned rather than floored. Direct dependencies only, at the versions that resolved — a
# full transitive freeze is also reproducible, but this file is meant to be read as well as
# installed.
#
# Resolved 2026-08-14 on Python 3.14.
sentence-transformers==5.7.0
faiss-cpu==1.15.0
numpy==2.5.2
