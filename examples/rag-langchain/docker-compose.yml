version: '3.8'
services:
  rag-service:
    build: .
    environment:
      # set your OPENAI_API_KEY here to enable LLM answers
      # OPENAI_API_KEY: ${OPENAI_API_KEY}
    stdin_open: true
    tty: true
