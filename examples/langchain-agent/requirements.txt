# Pinned rather than floored. This example previously carried `langchain>=0.0.300`, which
# resolved to a line where its own imports no longer exist — see docs/REPO-AUDIT.md task 11.
#
# Two distributions rather than one: `langchain_openai` was split out of the monolithic
# `langchain` package, and the old top-level package is not needed here at all.
#
# Resolved 2026-08-14 on Python 3.14.
langchain-core==1.5.4
langchain-openai==1.5.1
