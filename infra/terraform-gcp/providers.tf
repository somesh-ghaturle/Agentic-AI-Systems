provider "google" {}

# Note: Configure authentication via Application Default Credentials or a service account.
# Examples:
#  - `gcloud auth application-default login` (CLI)
#  - set `GOOGLE_APPLICATION_CREDENTIALS` to a service account key file
#  - Workload Identity Federation from CI, which avoids a long-lived key entirely
