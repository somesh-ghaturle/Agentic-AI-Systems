provider "azurerm" {
  features {}
}

# Note: Configure authentication via environment variables or Azure CLI.
# Examples:
#  - `az login` (CLI)
#  - set `ARM_CLIENT_ID`, `ARM_CLIENT_SECRET`, `ARM_TENANT_ID`, `ARM_SUBSCRIPTION_ID`
