<!--
  Copy to your repo root as context.md.
  This is the file that lets a fresh session resume instead of restarting.
  Overwrite stale entries — this is living state, not a log.

  SHARED REPO: gitignore this file. It is per-task, per-person state — committed to a
  repo other people work in, it conflicts on nearly every merge. See TEAM-WORKFLOW.md.
-->

# Context

**Last updated:** `<YYYY-MM-DD>`

## Where things stand

Two or three sentences. What is the current state of this work, right now.

## Decisions made

Record the *reasoning*, not just the outcome — the alternative and why it lost is the part
that stops the decision being relitigated next week.

| Decision | Reason | Rejected alternative |
|---|---|---|
| `<what we chose>` | `<why>` | <what we did not choose, and why not> |

## Constraints discovered

Things that turned out to be true and shaped the approach. Non-obvious facts about the
system that cost someone time to learn.

- …

## Dead ends

Approaches that did not work, and why. This section exists purely so nobody retries them —
including a future session of the agent.

- **Tried:** `<approach>` → **Failed because:** `<reason>`

## Open questions

| Question | Who or what can answer it | Assumption in the meantime |
|---|---|---|
| … | … | … |

## Environment notes

Setup facts that are not in the README and cost time to rediscover: services that must be
running, credentials needed, a step that fails confusingly without something else.

- …
