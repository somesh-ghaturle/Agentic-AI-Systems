<!--
  Copy to your repo root as tasks.md.
  Updated before every context clear. Keep it short — if it is long, it is a backlog, and
  backlogs belong in your issue tracker, not in agent context.
-->

# Tasks

**Last updated:** `<YYYY-MM-DD>`

## Next action

<The single next thing a fresh session should do. One line. This is the most important
field in the file — if you fill in nothing else, fill in this.>

---

## In progress

- [ ] **`<task>`** — <exact file and function, and what state it is in right now>
  - Done so far: `<what is finished>`
  - Remaining: `<what is not>`
  - Verified by: <command, and whether it currently passes>

## Blocked

- [ ] **`<task>`** — blocked on `<what>`
  - Unblocked by: <person, decision, or event>
  - Workaround in the meantime: <or "none">

## Done

- [x] **`<task>`** — verified by `<command>` → `<result>`
- [x] **`<task>`** — verified by `<command>` → `<result>`

## Deliberately not doing

Things considered and declined, so they do not get re-proposed every session.

- `<thing>` — because `<reason>`
